Commit all current changes and push to GitHub.

1. Run `git status` to see what's changed
2. Stage all relevant files (exclude any .env files or secrets)
3. Create a descriptive commit message based on the changes
4. Push to main

Do NOT commit: .env files, API keys, or any secrets. This repo is public.
